=============================
private:
    Chave nullKey;
public:
    vetorDes(string nome_arquivo, Item nullItem, Chave nullKey);

vetorDes<Chave, Item>::vetorDes
(string nome_arquivo, Item nullItem, Chave nullKey)
st(new par[2]), size(2), n(0), nullItem(nullItem), nullKey(nullKey)

---> Corpo da função construtora

devolve -> retornar NULLKEY

seleciona (exceção)
    -> return nullKey;
=============================
g++ -Wall -g -O0 -c ep1.cpp


Compilando o EP1:
g++ -std=c++11 -Wall -g -O0 -c ep1.cpp
g++ -std=c++11 -Wall -g -O0 -c unit_test.cpp
g++ -Wall -g -O0 -c util.cpp
g++ util.o ep1.o unit_test.o -o unit_test -lm

g++ -Wall -g -O0 -c unit_test.cpp


--------
minuscula

=============================
Arquivo para testes pessoais:
g++ -std=c++11 -Wall -g -O0 ep1.cpp -o ep1

g++ -std=c++11 -Wall -g -O0 unit_test.cpp util.cpp -o teste
./teste texto.txt VD
argv[0]

=============================
Relatório

- estrutura da pasta
- como compilar o ep1
- Alterações na inteface:
    - alterei no uso da função "testeOperacoes" os tipos
        String para string: motivo -> utilizei comparadores padrão (<, >, ==) para Chave, nos meus templates.
        Int para int
        -> Para isso, utilizei constantes quando necessário para os valores NULL
        NULLITEM: -1 (quanto int)
        NULLKEY: "NULL"

        Dentro das estruturas de dados, foram definidos os atributos que devem ser passado para o construtor:
        Item nullItem;
        Chave nullKey;

        key == NULL --> key == NULLKEY

        String key = st->seleciona(0); -> string key = st->seleciona(0);

        String key = getNextToken(NULL); ---> converte para string, que é o tipo do parâmetro do método 'seleciona'
            String paramChar = getNextToken(NULL);
            string key = convertToString(paramChar);

        int pos = atoi(key);
            ->> int pos = atoi(paramChar);

        Integer frequencia = NULL;
            --> int frequencia = NULLITEM;

        frequencia == NULL --> frequencia == NULLITEM

        *frequencia -> frequencia

        String chave = st->seleciona(pos);
            --> string chave = st->seleciona(pos);

        O código de unit_test foi alterado para utilizar as constantes também.
